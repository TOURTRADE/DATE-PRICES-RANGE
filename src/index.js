import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';  // Ensure that this path is correct
import App from './App';  // Ensure this path matches your file structure

ReactDOM.render(
  <App />,
  document.getElementById('root')
);
